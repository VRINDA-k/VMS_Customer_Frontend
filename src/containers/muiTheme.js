import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#43B749", contrastText: "#fff" },
  secondary: { main: "#009688", contrastText: "#fff" }
};
const themeName = "Apple Apple Caracal";

export default createMuiTheme({ palette, themeName });
